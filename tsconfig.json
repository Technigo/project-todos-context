{
  "compilerOptions": {
    /* Basic Settings */
    "target": "ESNext", /* Use the latest JavaScript version for modern browsers. */
    "module": "ESNext", /* Use ES Modules for modern JavaScript workflows. */
    "moduleResolution": "Node", /* Use Node.js module resolution strategy. */
    "esModuleInterop": true, /* Allow default imports from CommonJS modules. */
    "forceConsistentCasingInFileNames": true, /* Enforce consistent file name casing. */
    "strict": true, /* Enable all strict type-checking options. */
    "skipLibCheck": true, /* Skip type checking for all declaration files (.d.ts). */
    /* React Settings */
    "jsx": "react-jsx", /* Use the modern JSX runtime. */
    /* Additional Settings */
    "resolveJsonModule": true, /* Allow importing JSON files. */
    "allowSyntheticDefaultImports": true, /* Allow default imports from modules without default exports. */
    "isolatedModules": true, /* Ensure each file can be transpiled independently. */
    /* Paths & Directories */
    "baseUrl": "./", /* Base directory for resolving non-relative imports. */
    "paths": {
      "@components/*": [
        "src/components/*"
      ], /* Alias for components folder. */
      "@store/*": [
        "src/store/*"
      ] /* Alias for store folder. */
    },
    "rootDir": "./src", /* Root folder for TypeScript files. */
    "outDir": "./dist", /* Output folder for compiled JavaScript files. */
    /* Type Checking */
    "noImplicitAny": true, /* Disallow implicit 'any' types. */
    "strictNullChecks": true, /* Enable strict checking for `null` and `undefined`. */
    "noUnusedLocals": true, /* Warn about unused local variables. */
    "noUnusedParameters": true, /* Warn about unused parameters. */
    "noImplicitReturns": true, /* Ensure all code paths return a value. */
    "noFallthroughCasesInSwitch": true /* Prevent fallthrough cases in switch statements. */
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx"
  ], /* Include all TypeScript and TSX files in the `src` folder. */
  "exclude": [
    "node_modules",
    "dist",
    "src/**/*.js"
  ] /* Exclude `node_modules`, build files, and JavaScript files. */
}